namespace BDM.UI
{
    public interface IMenu //In all honesty, this interface isn't very useful at the moment (for this project anyway)
    {
        void OpenMenu();
        void CloseMenu();
    }
}